import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String inputData = scanner.nextLine(); // nextLine: 엔터 누르기 전까지 전체 문자열을 읽어라

        System.out.println(inputData);
    }
}